name: Deploy

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted

    strategy:
      matrix:
        app: [client, server]
        node-version: [20]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}

      - name: Fetch changes
        run: git fetch origin main
        working-directory: /var/www/pondus_app

      - name: Check for client changes
        if: ${{ matrix.app == 'client' }}
        id: client_changes
        run: |
          if git diff --name-only origin/main HEAD -- subdir/client | grep .; then
            echo "changes_detected=true" >> $GITHUB_ENV
          else
            echo "changes_detected=false" >> $GITHUB_ENV
          fi

      - name: Check for server changes
        if: ${{ matrix.app == 'server' }}
        id: server_changes
        run: |
          if git diff --name-only origin/main HEAD -- subdir/server | grep .; then
            echo "changes_detected=true" >> $GITHUB_ENV
          else
            echo "changes_detected=false" >> $GITHUB_ENV
          fi

      - name: Merge changes
        run: git merge origin/main
        working-directory: /var/www/pondus_app

      - name: Cache dependencies
        uses: actions/cache@v4
        with:
          path: |
            /var/www/pondus_app/${{ matrix.app }}/node_modules
            /root/.npm
          key: ${{ runner.os }}-node-${{ matrix.app }}-${{ matrix.node-version }}-${{ hashFiles('subdir/${{ matrix.app }}/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-${{ matrix.app }}-${{ matrix.node-version }}-

      - name: Install dependencies
        if: env.changes_detected == 'true'
        run: npm ci
        working-directory: /var/www/pondus_app/${{ matrix.app }}

      - name: Build client
        if: ${{ matrix.app == 'client' && env.changes_detected == 'true' }}
        run: npm run build
        working-directory: /var/www/pondus_app/client

      - name: Restart server
        if: ${{ matrix.app == 'server' && env.changes_detected == 'true' }}
        run: pm2 restart "Pondus Server"
